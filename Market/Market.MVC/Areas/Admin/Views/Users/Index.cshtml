@model Market.MVC.Areas.Admin.Models.AdminUsersVM
@{
    Layout = "_Layout";
}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card bg-transparent">
                <div class="table-responsive">
                    <table class="contact-list-table text-nowrap bg-white">
                        <thead>
                            <tr>
                                <th>Email</th>
                                <th>Имя</th>
                                <th>Фамилия</th>
                                <th>Пол</th>
                                <th>Телефон</th>
                                <th>Действия</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var user in Model.Users)
                            {
                                <tr id="user-@user.Id">
                                    <td>@user.Email</td>
                                    <td>@user.FirstName</td>
                                    <td>@user.LastName</td>
                                    <td>@user.Gender</td>
                                    <td>@user.Phone</td>
                                    <td class="actions">
                                        <button class="btn btn-primary btn-sm">
                                            Действия
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div> 